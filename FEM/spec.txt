ћои вопросы к „ехову:
*. может как-то вынести real_t за пределы класса, отвечающего за интегрирование, чтобы можно было извне задавать этот параметр?
*. что такое квадратура √аусса

ћои действи€ на сегодн€:
*. 

** GaussIntegr.hpp - выполн€ет процедуру численного интегрировани€ [-1,1].

** tests3dIntegr.cpp - пример интегрировани€ разных видов функций дл€ трЄхмерного случа€.

** ƒл€ интегрировани€ нужно создать объект класса fIntegrate<> с указанием в аргументах шаблона размерности пространства (1-отрезок,2-квадрат,3-куб) и пор€дка квадратуры (больше единицы, достаточно 2 или 3).

** ќн может интегрировать произвольные скал€рные функции или вектор-функции (главное чтобы существовали соответствующие типы данных) - дл€ этого у него есть методы ByPlusAssgn (дл€ суммировани€ испольует операцию +=) и ByPlus (использует +).

** –азновидности этих методов с окончанием _ArrArg дл€ интегрировани€ функций, у которых аргументы передаютс€ через массив (указатель).

** ¬ прилагаемой статье в разделе "„исленные исследовани€" описаны результаты работы этого модул€. ѕримерно такого типа исследовани€ предстоит проделать с разрабатываемой программой.


*. от чего будет зависеть мой шаблонный класс  - от точки или от €вно заданного типа пространства?
*. допилить FEModel.SetParams(argc, argv);
*. сделать FEModel.ReadData(FEModel.GetInputStream(), FEM::IO_FORMAT::CHEKHOV);
